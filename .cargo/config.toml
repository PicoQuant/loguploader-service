# Default to the shipped target so `cargo build` / `cargo test` on a dev box
# match what CI produces. Override with `--target` for cross-compilation.
[build]
target = "x86_64-pc-windows-msvc"

[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
