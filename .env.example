# Local testing secrets. Copy to .env and fill in real values. .env is gitignored.
#
# Fleet submission tokens for api.picoquant.com (see specs/003-backend-api-support).
# One non-expiring token per product; the backend accepts a comma-separated list for rotation.
TELEMETRY_FLEET_TOKENS_LUMINOSA=replace-me
# TELEMETRY_FLEET_TOKENS_SOLIRA=replace-me
